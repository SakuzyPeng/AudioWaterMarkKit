<Page
    x:Class="AWMKit.Pages.EmbedPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:AWMKit.Models"
    mc:Ignorable="d"
    Loaded="Page_Loaded">
    <Page.Resources>
        <Style x:Key="EmbedMainCardBorderStyle" TargetType="Border" BasedOn="{StaticResource CardBorderStyle}">
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="EmbedHeaderBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource ControlFillColorSecondaryBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="2,2,2,0"/>
        </Style>

        <Style x:Key="EmbedSettingCellBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource ControlFillColorSecondaryBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="2,0,2,0"/>
            <Setter Property="MinHeight" Value="64"/>
        </Style>
    </Page.Resources>

    <Grid Padding="24" RowSpacing="12" ColumnSpacing="12" Background="{ThemeResource PageBackdropBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource EmbedMainCardBorderStyle}" Padding="18">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="{ThemeResource ControlFillColorSecondaryBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12"
                        Margin="2,2,2,0">
                    <StackPanel Spacing="12">
                        <Button Click="InputSummaryButton_Click"
                                AutomationProperties.Name="{x:Bind ViewModel.SelectInputSourceAccessibility, Mode=OneWay}"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                Padding="0"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Stretch">
                            <StackPanel Spacing="4">
                                <TextBlock Text="{x:Bind ViewModel.InputSourceLabel, Mode=OneWay}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind ViewModel.InputSourceText, Mode=OneWay}"
                                           TextWrapping="Wrap"
                                           MaxLines="2"
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Button>
                        <Rectangle Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}"/>
                        <Button Click="OutputSummaryButton_Click"
                                AutomationProperties.Name="{x:Bind ViewModel.SelectOutputDirectoryAccessibility, Mode=OneWay}"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                Padding="0"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Stretch">
                            <StackPanel Spacing="4">
                                <TextBlock Text="{x:Bind ViewModel.OutputDirectoryLabel, Mode=OneWay}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind ViewModel.OutputDirectoryText, Mode=OneWay}"
                                           TextWrapping="Wrap"
                                           MaxLines="2"
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>

                <InfoBar Grid.Row="1"
                         IsClosable="False"
                         Severity="Warning"
                         Visibility="{x:Bind AppState.KeyAvailable, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                         Message="{x:Bind ViewModel.MissingKeyMessage, Mode=OneWay}">
                    <InfoBar.ActionButton>
                        <Button Content="{x:Bind ViewModel.GoToKeyPageText, Mode=OneWay}" Click="GoToKeyPageButton_Click"/>
                    </InfoBar.ActionButton>
                </InfoBar>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center" Margin="0,2,0,0">
                    <Button Command="{x:Bind ViewModel.ClearInputSourceCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            AutomationProperties.Name="{x:Bind ViewModel.ClearInputSourceAccessibility, Mode=OneWay}">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <SymbolIcon Symbol="OpenFile"/>
                            <TextBlock Text="{x:Bind ViewModel.ClearActionText, Mode=OneWay}"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{x:Bind ViewModel.ClearOutputDirectoryCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            AutomationProperties.Name="{x:Bind ViewModel.ClearOutputDirectoryAccessibility, Mode=OneWay}">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <SymbolIcon Symbol="Folder"/>
                            <TextBlock Text="{x:Bind ViewModel.ClearActionText, Mode=OneWay}"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{x:Bind ViewModel.EmbedOrStopCommand}"
                            IsEnabled="{x:Bind ViewModel.CanEmbedOrStop, Mode=OneWay}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            AutomationProperties.Name="{x:Bind ViewModel.EmbedActionAccessibility, Mode=OneWay}">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <SymbolIcon Symbol="Play"
                                        Visibility="{x:Bind ViewModel.ShowEmbedDefaultPlayIcon, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <SymbolIcon Symbol="Play"
                                        Foreground="{StaticResource SuccessBrush}"
                                        Visibility="{x:Bind ViewModel.ShowEmbedSuccessPlayIcon, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <SymbolIcon Symbol="Stop"
                                        Foreground="{StaticResource ErrorBrush}"
                                        Visibility="{x:Bind ViewModel.ShowEmbedStopIcon, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <TextBlock Text="{x:Bind ViewModel.EmbedButtonText, Mode=OneWay}"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{x:Bind ViewModel.ClearQueueCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            AutomationProperties.Name="{x:Bind ViewModel.ClearQueueAccessibility, Mode=OneWay}">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <SymbolIcon Symbol="List"
                                        Visibility="{x:Bind ViewModel.IsClearQueueSuccess, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                            <SymbolIcon Symbol="List"
                                        Foreground="{StaticResource SuccessBrush}"
                                        Visibility="{x:Bind ViewModel.IsClearQueueSuccess, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <TextBlock Text="{x:Bind ViewModel.ClearActionText, Mode=OneWay}"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{x:Bind ViewModel.ClearLogsCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            AutomationProperties.Name="{x:Bind ViewModel.ClearLogsAccessibility, Mode=OneWay}">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <SymbolIcon Symbol="Message"
                                        Visibility="{x:Bind ViewModel.IsClearLogsSuccess, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                            <SymbolIcon Symbol="Message"
                                        Foreground="{StaticResource SuccessBrush}"
                                        Visibility="{x:Bind ViewModel.IsClearLogsSuccess, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <TextBlock Text="{x:Bind ViewModel.ClearActionText, Mode=OneWay}"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <Border Grid.Row="3"
                        x:Name="DropZone"
                        AllowDrop="True"
                        DragOver="DropZone_DragOver"
                        Drop="DropZone_Drop"
                        BorderThickness="1"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        CornerRadius="12"
                        Padding="20"
                        Margin="2,0,2,2"
                        Background="{ThemeResource ControlFillColorSecondaryBrush}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="6">
                        <SymbolIcon Symbol="Audio"/>
                        <TextBlock Text="{x:Bind ViewModel.DropZoneTitle, Mode=OneWay}" FontWeight="SemiBold" TextAlignment="Center"/>
                        <TextBlock Text="{x:Bind ViewModel.DropZoneSubtitle, Mode=OneWay}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   TextAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource EmbedMainCardBorderStyle}" Padding="18">
            <Grid RowSpacing="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource EmbedHeaderBorderStyle}">
                    <TextBlock Text="{x:Bind ViewModel.SettingsTitle, Mode=OneWay}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                </Border>

                <Border Grid.Row="1" Style="{StaticResource EmbedSettingCellBorderStyle}">
                    <StackPanel Spacing="4">
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{x:Bind ViewModel.UsernameLabel, Mode=OneWay}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="0">
                                <TextBlock Text="{x:Bind ViewModel.ReuseHintText, Mode=OneWay}"
                                           Foreground="{x:Bind ViewModel.PreviewTagBrush, Mode=OneWay}"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="{x:Bind ViewModel.PreviewTagDisplay, Mode=OneWay}"
                                           Foreground="{x:Bind ViewModel.PreviewTagBrush, Mode=OneWay}"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                        <TextBox Text="{x:Bind ViewModel.UsernameInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 PlaceholderText="{x:Bind ViewModel.UsernamePlaceholder, Mode=OneWay}"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="2"
                        Style="{StaticResource EmbedSettingCellBorderStyle}">
                    <StackPanel Spacing="6">
                        <TextBlock Text="{x:Bind ViewModel.StoredMappingsLabel, Mode=OneWay}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <ComboBox ItemsSource="{x:Bind ViewModel.MappingSuggestions, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedMapping, Mode=TwoWay}"
                                  PlaceholderText="{x:Bind ViewModel.MappingPlaceholderText, Mode=OneWay}"
                                  IsEnabled="{x:Bind ViewModel.HasMappings, Mode=OneWay}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate x:DataType="models:EmbedMappingOption">
                                    <TextBlock Text="{x:Bind DisplayText}" TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>
                </Border>

                <Border Grid.Row="3" Style="{StaticResource EmbedSettingCellBorderStyle}">
                    <StackPanel Spacing="6">
                        <TextBlock Text="{x:Bind ViewModel.StrengthLabel, Mode=OneWay}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Text="{x:Bind ViewModel.Strength, Mode=OneWay}"/>
                        <Slider Minimum="1"
                                Maximum="30"
                                Value="{x:Bind ViewModel.Strength, Mode=TwoWay}"
                                TickFrequency="1"
                                SnapsTo="Ticks"
                                Margin="0,-15,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="4" Style="{StaticResource EmbedSettingCellBorderStyle}">
                    <StackPanel Spacing="6">
                        <TextBlock Text="{x:Bind ViewModel.SuffixLabel, Mode=OneWay}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBox Text="{x:Bind ViewModel.CustomSuffix, Mode=TwoWay}" PlaceholderText="_wm"/>
                    </StackPanel>
                </Border>

            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource EmbedMainCardBorderStyle}" Padding="18">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource EmbedHeaderBorderStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind ViewModel.QueueTitle, Mode=OneWay}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Grid.Column="1"
                                   VerticalAlignment="Center"
                                   Text="{x:Bind ViewModel.QueueCountText, Mode=OneWay}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Visibility="{x:Bind ViewModel.HasQueueCount, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </Grid>
                </Border>

                <Grid Grid.Row="1" ColumnSpacing="8" Margin="2,0,2,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="40"/>
                    </Grid.ColumnDefinitions>
                    <ProgressBar Grid.Column="0" Value="{x:Bind ViewModel.ProgressPercent, Mode=OneWay}" Maximum="100" Height="8"/>
                    <TextBlock Grid.Column="1"
                               Text="{x:Bind ViewModel.ProgressPercentText, Mode=OneWay}"
                               HorizontalAlignment="Right"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </Grid>

                <TextBlock Grid.Row="2"
                           Text="{x:Bind ViewModel.QueueEmptyText, Mode=OneWay}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Margin="0,8,0,0"
                           Visibility="{x:Bind ViewModel.ShowQueueEmptyHint, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>

                <ListView Grid.Row="2"
                          ItemsSource="{x:Bind ViewModel.SelectedFiles, Mode=OneWay}"
                          SelectionMode="None"
                          Margin="2,6,2,2"
                          Visibility="{x:Bind ViewModel.QueueCount, Mode=OneWay, Converter={StaticResource CountToVisibilityConverter}}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <Border Style="{StaticResource ListItemBorderStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{x:Bind}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                    <Button Grid.Column="1"
                                            Click="RemoveQueueFileButton_Click"
                                            Tag="{x:Bind}"
                                            Background="Transparent"
                                            BorderThickness="0">
                                        <SymbolIcon Symbol="Delete"/>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource EmbedMainCardBorderStyle}" Padding="18">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource EmbedHeaderBorderStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind ViewModel.LogsTitle, Mode=OneWay}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <ToggleSwitch Grid.Column="1"
                                      Header="{x:Bind ViewModel.ShowDiagnosticsText, Mode=OneWay}"
                                      IsOn="{x:Bind ViewModel.ShowDiagnostics, Mode=TwoWay}"
                                      Margin="0,0,8,0"/>
                        <TextBlock Grid.Column="2"
                                   VerticalAlignment="Center"
                                   Text="{x:Bind ViewModel.LogCountText, Mode=OneWay}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Visibility="{x:Bind ViewModel.HasLogs, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </Grid>
                </Border>

                <TextBlock Grid.Row="1"
                           Text="{x:Bind ViewModel.LogsEmptyText, Mode=OneWay}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Margin="0,8,0,0"
                           Visibility="{x:Bind ViewModel.ShowNoLogsHint, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>

                <ListView Grid.Row="1"
                          ItemsSource="{x:Bind ViewModel.Logs, Mode=OneWay}"
                          SelectionMode="None"
                          Margin="2,6,2,2"
                          Visibility="{x:Bind ViewModel.HasLogs, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:LogEntry">
                            <Border BorderBrush="{x:Bind CardBorderBrush}"
                                    BorderThickness="1"
                                    Background="{x:Bind CardBackgroundBrush}"
                                    CornerRadius="8"
                                    Padding="10"
                                    Margin="0,0,0,6">
                                <Grid RowSpacing="6">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <FontIcon Glyph="{x:Bind IconGlyph}" Foreground="{x:Bind IconBrush}" Margin="0,0,8,0"/>
                                        <TextBlock Grid.Column="1" Text="{x:Bind Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="2"
                                                   Text="{x:Bind Timestamp, Mode=OneWay, Converter={StaticResource DateTimeConverter}}"
                                                   FontSize="12"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </Grid>

                                    <TextBlock Grid.Row="1"
                                               Text="{x:Bind Detail}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               TextWrapping="WrapWholeWords"
                                               MaxLines="2"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Grid.Row="2"
                                               Text="{x:Bind DiagnosticDetail}"
                                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                               TextWrapping="WrapWholeWords"
                                               Visibility="{x:Bind RootPage.ViewModel.ShowDiagnostics, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
