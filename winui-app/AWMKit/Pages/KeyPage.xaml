<Page
    x:Class="AWMKit.Pages.KeyPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">
    <Page.Resources>
        <Style x:Key="KeyHeaderBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource ControlFillColorSecondaryBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="2,2,2,0"/>
        </Style>
    </Page.Resources>

    <Grid Padding="24" ColumnSpacing="12" Background="{ThemeResource PageBackdropBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="500"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Padding="20">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource KeyHeaderBorderStyle}">
                    <TextBlock Text="{x:Bind ViewModel.KeyPageTitle, Mode=OneWay}" Style="{StaticResource SectionHeaderStyle}" Margin="0"/>
                </Border>

                <InfoBar Grid.Row="2"
                         IsClosable="False"
                         Severity="{x:Bind ViewModel.KeyStatusSeverity, Mode=OneWay}"
                         Message="{x:Bind ViewModel.KeyStatusMessage, Mode=OneWay}"/>

                <Border Grid.Row="3" Style="{StaticResource ListItemBorderStyle}">
                    <StackPanel Spacing="8">
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="96"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{x:Bind ViewModel.KeyStatusFieldLabel, Mode=OneWay}"
                                       FontWeight="SemiBold"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Grid.Column="1"
                                       Text="{x:Bind ViewModel.KeyStatusText, Mode=OneWay}"
                                       Foreground="{x:Bind ViewModel.KeyStatusBrush, Mode=OneWay}"/>
                        </Grid>
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="96"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{x:Bind ViewModel.KeySourceFieldLabel, Mode=OneWay}"
                                       FontWeight="SemiBold"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Grid.Column="1"
                                       Text="{x:Bind ViewModel.KeySourceLabel, Mode=OneWay}"
                                       TextWrapping="WrapWholeWords"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Row="4" Style="{StaticResource ListItemBorderStyle}">
                    <StackPanel Spacing="8">
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="96"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{x:Bind ViewModel.ActiveSlotFieldLabel, Mode=OneWay}"
                                       FontWeight="SemiBold"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
                                <ComboBox Width="120"
                                          ItemsSource="{x:Bind ViewModel.SlotOptions, Mode=OneWay}"
                                          SelectedItem="{x:Bind ViewModel.SelectedSlot, Mode=TwoWay}"/>
                                <Button Click="ApplySlotButton_Click"
                                        IsEnabled="{x:Bind ViewModel.CanOperate, Mode=OneWay}">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <SymbolIcon Symbol="AllApps"
                                                    Foreground="{x:Bind ViewModel.ApplyActionBrush, Mode=OneWay}"/>
                                        <TextBlock Text="{x:Bind ViewModel.ApplyActionText, Mode=OneWay}"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                        <TextBlock Text="{x:Bind ViewModel.ActiveKeySlotText, Mode=OneWay}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="5" Style="{StaticResource ListItemBorderStyle}">
                    <StackPanel Spacing="4">
                        <TextBlock Text="{x:Bind ViewModel.ActiveKeySectionTitle, Mode=OneWay}"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <SymbolIcon Symbol="AllApps"
                                        Foreground="{x:Bind ViewModel.ActiveSummaryTitleBrush, Mode=OneWay}"/>
                            <TextBlock Text="{x:Bind ViewModel.ActiveSummaryTitle, Mode=OneWay}"
                                       FontWeight="SemiBold"
                                       Foreground="{x:Bind ViewModel.ActiveSummaryTitleBrush, Mode=OneWay}"/>
                        </StackPanel>
                        <TextBlock Text="{x:Bind ViewModel.ActiveSummaryKeyLine, Mode=OneWay}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{x:Bind ViewModel.ActiveSummaryEvidenceLine, Mode=OneWay}"
                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                   FontSize="12"/>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="6" Spacing="10">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Click="GenerateKeyButton_Click"
                                Style="{StaticResource PrimaryButtonStyle}"
                                IsEnabled="{x:Bind ViewModel.CanGenerateKey, Mode=OneWay}">
                            <ToolTipService.ToolTip>
                                <TextBlock Text="{x:Bind ViewModel.GenerateKeyTooltip, Mode=OneWay}"/>
                            </ToolTipService.ToolTip>
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <SymbolIcon Symbol="Permissions"
                                            Foreground="{x:Bind ViewModel.GenerateActionBrush, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.GenerateActionText, Mode=OneWay}"/>
                            </StackPanel>
                        </Button>

                        <Button Click="ImportKeyButton_Click"
                                Style="{StaticResource SecondaryButtonStyle}"
                                IsEnabled="{x:Bind ViewModel.CanImportKey, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <SymbolIcon Symbol="Download"
                                            Foreground="{x:Bind ViewModel.ImportActionBrush, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.ImportFileActionText, Mode=OneWay}"/>
                            </StackPanel>
                        </Button>

                        <Button Click="ImportHexButton_Click"
                                Style="{StaticResource SecondaryButtonStyle}"
                                IsEnabled="{x:Bind ViewModel.CanImportKey, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <SymbolIcon Symbol="Document"
                                            Foreground="{x:Bind ViewModel.ImportHexActionBrush, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.ImportHexActionText, Mode=OneWay}"/>
                            </StackPanel>
                        </Button>

                        <Button Click="ExportKeyButton_Click"
                                Style="{StaticResource SecondaryButtonStyle}"
                                IsEnabled="{x:Bind ViewModel.CanExportKey, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <SymbolIcon Symbol="Upload"
                                            Foreground="{x:Bind ViewModel.ExportActionBrush, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.ExportFileActionText, Mode=OneWay}"/>
                            </StackPanel>
                        </Button>

                        <Button Click="EditLabelButton_Click"
                                Style="{StaticResource SecondaryButtonStyle}"
                                IsEnabled="{x:Bind ViewModel.CanOperate, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <SymbolIcon Symbol="Tag"
                                            Foreground="{x:Bind ViewModel.EditActionBrush, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.EditActionText, Mode=OneWay}"/>
                            </StackPanel>
                        </Button>

                        <Button Click="DeleteKeyButton_Click"
                                Style="{StaticResource SecondaryButtonStyle}"
                                IsEnabled="{x:Bind ViewModel.SelectedSlotHasKey, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <SymbolIcon Symbol="Delete"
                                            Foreground="{x:Bind ViewModel.DeleteActionBrush, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.DeleteActionText, Mode=OneWay}"/>
                            </StackPanel>
                        </Button>

                        <Button Click="RefreshButton_Click"
                                Style="{StaticResource SecondaryButtonStyle}"
                                IsEnabled="{x:Bind ViewModel.CanOperate, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <SymbolIcon Symbol="Refresh"
                                            Foreground="{x:Bind ViewModel.RefreshActionBrush, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.RefreshActionText, Mode=OneWay}"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}" Padding="20">
            <Grid RowSpacing="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource KeyHeaderBorderStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{x:Bind ViewModel.SlotSummaryTitle, Mode=OneWay}"
                                   Style="{StaticResource SectionHeaderStyle}"
                                   Margin="0"/>

                        <Border Grid.Column="1"
                                Background="{ThemeResource ControlFillColorSecondaryBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="8,4"
                                VerticalAlignment="Center"
                                Visibility="{x:Bind ViewModel.ShowConfiguredSlotCount, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="{x:Bind ViewModel.ConfiguredSlotCountText, Mode=OneWay}"
                                       FontWeight="SemiBold"/>
                        </Border>
                    </Grid>
                </Border>

                <TextBox Grid.Row="1"
                         PlaceholderText="{x:Bind ViewModel.SlotSearchPlaceholder, Mode=OneWay}"
                         Text="{x:Bind ViewModel.SlotSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <ListView Grid.Row="2"
                          ItemsSource="{x:Bind ViewModel.FilteredSlotSummaries, Mode=OneWay}"
                          SelectionMode="None"
                          IsItemClickEnabled="False"
                          ScrollViewer.VerticalScrollMode="Enabled"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollMode="Disabled"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Spacing="2" Padding="8,6">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <SymbolIcon Symbol="AllApps"
                                                Foreground="{Binding IsActive, Converter={StaticResource BoolToKeySlotIconBrushConverter}}"/>
                                    <TextBlock Text="{Binding TitleWithStatus}"
                                               FontWeight="SemiBold"
                                               Foreground="{Binding StatusText, Converter={StaticResource KeySlotStatusBrushConverter}}"/>
                                </StackPanel>
                                <TextBlock Text="{Binding KeyLine}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{Binding EvidenceLine}"
                                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                           FontSize="12"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
